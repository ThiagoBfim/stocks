language: java

os: linux
dist: trusty
jdk: oraclejdk11

before_install:
  - chmod +x mvnw
jobs:
  include:
    - stage: "Build project"
      script:
        - ./mvn clean package

deploy:
  provider: heroku
  app: dio-cities-api-2
  api_key: $HEROKU_KEY
